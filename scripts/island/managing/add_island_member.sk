#
# Játékos hozzáadása a sziget tagjai közé
#
#    Args:
#        p (Player): A hozzáadni kívánt játékos
#        id (integer): A sziget azonosítója
#
function add_island_member(p: player, ID: integer):

	set {island::player::%uuid of {_p}%::ID} to {_ID}
	add {_p} to {island::data::%{_ID}%::members::*}