function delete_island_data(ID: text):

	set {_collection} to get_collection("island")
	set {_document} to get_document("island", "uuid", {_ID})

	remove mongo documents {_document} from {_collection}