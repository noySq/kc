#
# Sziget világ törlése
#
#    Args:
#        ID (integer): A sziget azonosítója
#
function delete_world(ID: integer):

	set {_world} to get_island_world({_ID})

	if {_world} is not set:
		stop

	unload world {_world}
	delete world file for {_world}