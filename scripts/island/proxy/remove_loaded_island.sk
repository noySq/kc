function remove_loaded_island(ID: text):

    set {_hostname} to get_hostname()
    set {_channel} to join ({-config::island::redis::list::loaded-islands}, {_hostname})
    remove_from_redis_list({_ID}, {_channel})