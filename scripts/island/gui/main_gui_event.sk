on inventory click:

	if event-inventory = {island::gui::main}:

		cancel event
		set {_ID} to get_player_island_id(player)

		switch index of event-slot:
			case 19:
				create_island(player)
			case 21:
				teleport_player(player, {_ID})
			case 23:
				broadcast indices of {island::data::*}
			case 25:
				broadcast "wip"