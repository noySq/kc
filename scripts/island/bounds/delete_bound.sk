#
# Sziget bound törlése
#
#    Args:
#        id (integer): A sziget azonosítója
#
function delete_bound(ID: integer):

    set {_bound} to get_bound({_ID})
    set {_bound_ID} to bound id of {_bound}
    delete bound with id {_bound_ID}