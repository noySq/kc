function get_displayname(p: offlineplayer) :: text:

	if {_p} is not set:
		return "&c&lSZERVER"

	set {_name::1} to get_player_rank({_p})
	set {_name::2} to get_player_name({_p})

	return join {_name::*} with " "