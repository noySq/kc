function delete_rank(rank: text):

	set {_document} to get_document({-config::rank::collection::rank}, "rank", {_rank})
	if {_document} is set:
		remove mongo documents {_document} from get_collection({-config::rank::collection::rank})
		delete_rank_team({_rank})