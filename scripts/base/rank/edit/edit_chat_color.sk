function edit_chat_color(rank: text, color: text):

	set {_document} to get_document({-config::rank::collection::rank}, "rank", {_rank})
	if {_document} is set:

		set mongo value "chat_color" of {_document} to {_color}
		update mongo document {_document} into get_collection({-config::rank::collection::rank})