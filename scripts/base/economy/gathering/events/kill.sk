on death of mob with priority highest:

	if event is cancelled:
		stop

	if attacker is not player:
		stop

	set {_money} to calculate_money()
	give_money(attacker, {_money})

	show_temp_hologram(format_balance({_money}), head of victim)