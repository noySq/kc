function send_proxy_data(channel: text, data: text, server: text = "all"):

	set {_hostname} to get_hostname()
	set {_json} to "{""server"":{""from"":""%{_hostname}%"",""target"":""%{_server}%""},""message"":%{_data}%}"

	send custom redis message {_json} to channel {_channel}