function get_max_slot() :: integer:

    set {_slots} to max player count

    loop get_servers():
        if is_island_server(loop-value) = true:
            add 1 to {_server_count}

    return {_slots} * {_server_count}