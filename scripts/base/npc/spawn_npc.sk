function spawn_npc(location: location, npc: text):

	set {_type} to get_type({_npc})
	spawn {_type} at {_location}:

		set ai of entity to false
		make spawn_nametag(entity, {_npc}) ride entity

		set {-npc::spawned::%uuid of entity%} to {_npc}