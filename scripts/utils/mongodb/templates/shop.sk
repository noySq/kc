function shop_template(uuid: text) :: mongodocument:

	set {_collection} to get_collection("shop")

	set {_document} to a new mongo document with:
		mongo "uuid": {_uuid}
		mongo "item": null
		mongo "category": null
		mongo "purchase_price": null
		mongo "sell_price": null

	return {_document}