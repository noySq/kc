function get_players_on_server(server: text) :: offlineplayers:

	set {_channel} to join ({-config::redis::channel::online-players}, {_server})
	set {_uuids::*} to get_redis_list({_channel})

	loop {_uuids::*}:
		add (loop-value parsed as offlineplayer) to {_players::*}
	
	return {_players::*}
