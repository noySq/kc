on join:

	set_tablist_name(player)

	set {_guard} to unix timestamp of now
	set {-tablist::%uuid of player%::loop} to {_guard}

	while {-tablist::%uuid of player%::loop} is {_guard}:
		set_tablist(player)
		wait 1 second