function get_weight(rank: text) :: text:

	if {_rank} is not set:
		stop

	set {_document} to get_document({-config::rank::collection::rank}, "rank", {_rank})
	if {_document} is set:

		return mongo value "weight" of {_document}