on quit:

	set quit message to ""
	
	set {_uuid} to uuid of player
	set {_hostname} to get_hostname()

	remove_from_redis_list({_uuid}, "online-players")
	remove_from_redis_list({_uuid}, "server-players:%{_hostname}%")