on join:

	set join message to ""
	
	spawn(player)
	
	set {_uuid} to uuid of player
	set {_hostname} to get_hostname()

	add_to_redis_list({_uuid}, "online-players")
	add_to_redis_list({_uuid}, "server-players:%{_hostname}%")